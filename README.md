# OnlineSurveySystem
I had to export the code into a jar file to automatically run the system.
The demo recording would not record my actually going through the system after multiple trys. IF you just open the jar file it will open the system up and you can click through the tabs to create, and take the survey
